<UserControl x:Class="HardwareShopPro.UI.Views.SupplierListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <materialDesign:Card Grid.Row="0" Padding="16" Margin="0,0,0,12" UniformCornerRadius="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox materialDesign:HintAssist.Hint="Search suppliers..."
                         materialDesign:TextFieldAssist.HasLeadingIcon="True"
                         materialDesign:TextFieldAssist.LeadingIcon="Magnify"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Height="40" FontSize="13">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="12,0,0,0">
                    <Button Command="{Binding OpenAddDialogCommand}" Height="40"
                            Style="{StaticResource MaterialDesignRaisedButton}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center"/>
                            <TextBlock Text="Add" Margin="4,0,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding OpenEditDialogCommand}" Height="40" Margin="4,0"
                            Style="{StaticResource MaterialDesignOutlinedButton}">
                        <materialDesign:PackIcon Kind="Pencil"/>
                    </Button>
                    <Button Command="{Binding DeleteCommand}" Height="40" Foreground="IndianRed"
                            Style="{StaticResource MaterialDesignOutlinedButton}">
                        <materialDesign:PackIcon Kind="Delete"/>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="1" UniformCornerRadius="12" Padding="4">
            <DataGrid ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                      materialDesign:DataGridAssist.CellPadding="14,8" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Contact" Binding="{Binding Contact}" Width="140"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                    <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
                    <DataGridTextColumn Header="GSTIN" Binding="{Binding GSTIN}" Width="170"/>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>

        <!-- Add/Edit Dialog -->
        <materialDesign:Card Grid.Row="0" Grid.RowSpan="2"
                             Visibility="{Binding IsDialogOpen, Converter={StaticResource BooleanToVisibilityConverter}}"
                             Panel.ZIndex="10" HorizontalAlignment="Center" VerticalAlignment="Center"
                             Width="450" Padding="24" UniformCornerRadius="16">
            <StackPanel>
                <TextBlock FontSize="20" FontWeight="Bold" Margin="0,0,0,16"
                           Text="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter='Edit Supplier|Add Supplier'}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock"><Setter Property="Text" Value="Add Supplier"/>
                            <Style.Triggers><DataTrigger Binding="{Binding IsEditMode}" Value="True">
                                <Setter Property="Text" Value="Edit Supplier"/></DataTrigger></Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBox materialDesign:HintAssist.Hint="Name *" materialDesign:HintAssist.IsFloating="True"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding EditingSupplier.Name}" Margin="0,0,0,12"/>
                <TextBox materialDesign:HintAssist.Hint="Contact" materialDesign:HintAssist.IsFloating="True"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding EditingSupplier.Contact}" Margin="0,0,0,12"/>
                <TextBox materialDesign:HintAssist.Hint="Email" materialDesign:HintAssist.IsFloating="True"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding EditingSupplier.Email}" Margin="0,0,0,12"/>
                <TextBox materialDesign:HintAssist.Hint="Address" materialDesign:HintAssist.IsFloating="True"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding EditingSupplier.Address}" Margin="0,0,0,12"/>
                <TextBox materialDesign:HintAssist.Hint="GSTIN" materialDesign:HintAssist.IsFloating="True"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding EditingSupplier.GSTIN}" Margin="0,0,0,16"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Style="{StaticResource MaterialDesignOutlinedButton}"
                            Command="{Binding CancelDialogCommand}" Height="36" Margin="0,0,8,0"/>
                    <Button Content="Save" Style="{StaticResource MaterialDesignRaisedButton}"
                            Command="{Binding SaveCommand}" Height="36"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
